#include <stdio.h>

int main(void)
{
    /*
     * Ask the user for:
     * 1. name
     * 2. address
     * 3. phone number
     * 4. city
     * 5. state
     * 6. zip code
     * 7: college majore
     */

    char name[200];
    char address[200];
    char phone[200];
    char city[200];
    char state[200];
    char zip_code[200];
    char major[200];

    printf("Enter your name: ");
    gets(name);
    printf("Enter your phone number: ");
    gets(phone);
    printf("Enter your name: ");
    gets(name);
    printf("Enter your name: ");
    gets(name);
    printf("Enter your name: ");
    gets(name);
    printf("Enter your name: ");
    gets(name);
    printf("Enter your name: ");
    gets(name);
}